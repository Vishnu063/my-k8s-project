apiVersion: v1
kind: Service
metadata:
  name: devsecops-app
  namespace: devsecops-app
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 80
      nodePort: 30080
  selector:
    app: devsecops-app
